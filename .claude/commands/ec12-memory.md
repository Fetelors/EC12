Review the current conversation and identify anything worth saving to persistent memory.

Go through the conversation and look for:
- New decisions made (e.g. "we decided to use X instead of Y")
- New things learned about the system or Monday.com setup
- Preferences or feedback the user expressed
- New components or features that were scoped or defined
- Anything that would be confusing to re-explain next session

For each candidate, ask the user: "Should I save this to memory? [what it is in one line]"

Only save what the user confirms. Use the appropriate memory type (project, feedback, user, reference).
After saving, update MEMORY.md with a pointer to each new file.
